# konsol-exec orchestration plane — durable handoff (read me first)

You are a fresh agent enhancing the **existing `konsol-exec` Vite SPA** into the *single* execution plane (Airbyte/Press-style) that drives complete orchestrator runs. You start with no prior memory — this file + `PRDS-exec.md` are the source of truth. Do **exactly one PRD**, then update this file for the next agent.

## Goal (why this exists)
The orchestrator BACKEND is built and works (`konsol/orchestrator/*`, doctypes Pipeline Run/Step + Pipeline Definition/Schedule, whitelisted API `konsol.orchestrator.api.start_run/retry_step/resume_run/cancel_run`, live `orchestrator_step` realtime). A previous mistake built a throwaway **Desk page** at `/app/konsol-exec`; we are deleting that and instead enhancing the real SPA at **`konsol-exec/`** (served at `/konsol-exec/`) to: launch a run with params, show a **live stepped timeline** (status pills, durations, rows, logs), and **retry/resume/cancel** — one well-organized view.

## Where things are
- **Working dir:** `/home/pd/open_epm/docker/frappe/konsol`  (branch `feat/orchestrator-p1`, never commit to main)
- **The SPA:** `konsol-exec/src/` — Vite + React + XState. Key files: `api.js` (backend wrapper `frappeCall(method,args)`), `App.jsx` (nav: sections + subviews setup/monitor/history via `konsolAppMachine`), `machines/konsolAppMachine.js` + `machines/runDetailMachine.js`, `components/*.jsx` (Monitor, RunDetail, History, …), `constants.js`, `domain.js`.
- **New pure core you add:** `konsol-exec/src/orchestrator/*.js` (ESM, **no React import** — pure functions only).
- **Backend API (already there):** `konsol/orchestrator/api.py` — `start_run(definition, params)`, `retry_step(run_name, step_id)`, `resume_run(run_name, step_id)`, `cancel_run(run_name)`. Pipeline Run child table `steps` rows carry: `step_id, step_type, status, started_at, ended_at, rows, output, error`. Status vocab: Pending/Running/Success/Failed/Skipped/Cancelled.

## TDD harness (two kinds — match the PRD)
1. **Pure ESM core** (`konsol-exec/src/orchestrator/*.js`): write tests as `konsol-exec/src/orchestrator/<name>.test.mjs` using `node:test` + `node:assert`. Run from `konsol-exec/`: `node --test src/orchestrator/*.test.mjs`. Confirm RED first. **No npm install needed.**
2. **React / machine / api wiring** (can't build in-loop): write a **static-assertion** Python test in `konsol/tests/test_exec_<area>.py` that reads the source file(s) and asserts the required exports/JSX/strings/imports/method names are present (this matches the repo's existing `test_orchestrator_spa_js.py` convention). Run: `python3 -m pytest konsol/tests/test_exec_*.py -q` from the konsol root. Keep the real LOGIC in the pure ESM core so components stay thin.
3. **Backend additions** (if a PRD adds to `api.py`): guard frappe tests with `import pytest; frappe = pytest.importorskip("frappe")`.

Do NOT run `vite build` (node_modules may be absent / installing) — it is the maintainer's final gate, not yours.

## Loop protocol (one PRD, strict TDD)
1. Open `PRDS-exec.md`, take the first unchecked `- [ ] **E-N`.
2. RED: write the test (node --test for pure core, or pytest static-assert for wiring); run it; confirm it fails.
3. GREEN: implement; iterate until that test passes.
4. REGRESS: run BOTH suites — `cd konsol-exec && node --test src/orchestrator/*.test.mjs` (if any pure tests exist) AND `python3 -m pytest konsol/tests/test_exec_*.py konsol/tests/test_orchestrator_*.py -q`. All green.
5. COMMIT (format below), tick the PRD `- [x]` in `PRDS-exec.md`, rewrite this file's **Current state** + **Next**, commit docs too.
6. STOP.

## Conventions
- Commit subject: `feat(konsol-exec): E-N <short title> (konsol#60)` + body + `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.
- Pure core = no React, no fetch, no frappe — just data in/out (testable with node --test).
- Reuse existing app patterns (XState events, `frappeCall`, component style). Don't add new heavy deps.
- Bash safety: no `$()` inside other commands' args.
- Scope: only `konsol-exec/`, `konsol/orchestrator/api.py`, `konsol/tests/test_exec_*.py`, and (E9) removing the Desk page. Do NOT touch dbt_project or other repos.

## Current state (update me each PRD)
**Done:**
- **E1 — Run-params builder** (commit on `feat/orchestrator-p1`). New pure-ESM core dir `konsol-exec/src/orchestrator/`. `params.js` exports `buildRunArgs(form) -> {definition, params}` (definition or null; fiscal_year/fiscal_period/scope only when truthy; full_refresh/skip_sync always 0/1 ints) — mirrors the old Desk page `collect_params`. Unit test `params.test.mjs` (5 tests, node:test) green.
- **E2 — Status model** (commit `feat(konsol-exec): E2 status model (konsol#60)`). New pure-ESM `konsol-exec/src/orchestrator/status.js` exports `statusTone(status)` (Success/Completed→green, Failed/Cancelled→red, Running→blue, Queued→amber, Pending/Skipped→gray, default gray), `isTerminal(status)` (Completed/Failed/Cancelled/Success), `isRunning(status)` (Running only). Tolerates the realtime layer's Queued/Completed alongside the backend vocab. Unit test `status.test.mjs` (4 tests, node:test) green: every status→tone, default gray, terminal booleans, running boolean.
- **E3 — Run view-model** (commit `feat(konsol-exec): E3 run view-model (konsol#60)`, `2ace125`). New pure-ESM `konsol-exec/src/orchestrator/runModel.js` exports `normalizeRun(doc)` (maps a Pipeline Run doc + its snake_case `steps` child rows → `{name, status, steps:[{id,type,status,startedAt,endedAt,rows,output,error}]}`; tolerates null doc, missing `steps`→`[]`, missing fields → null / `rows`=0 / `output`,`error`=""), `progressPct(steps)` (terminal-success/total as 0–100, reuses `isTerminal` from `status.js`, 0 when empty), `orderSteps(steps)` (stable-ordered shallow copy, no mutation). Unit test `runModel.test.mjs` (4 tests, node:test) green: field mapping/camelCase, tolerance of sparse/null docs, progress math (mix→50, all-success→100, empty/Failed→0), stable non-mutating order.

- **E4 — Backend get_run + SPA api client** (commit `feat(konsol-exec): E4 backend get_run + SPA api client (konsol#60)`, `81b8a9e`). Backend: added whitelisted `get_run(run_name)` to `konsol/orchestrator/api.py` returning `{name, status, steps:[{step_id, step_type, status, started_at, ended_at, rows, output, error}]}` (read-only snapshot the SPA normalises via `runModel.normalizeRun`; imports without frappe like the rest of the module). SPA: added exec-plane client to `konsol-exec/src/api.js` — `startRun(definition, params)`, `getRun(name)`, `retryStep(name, stepId)`, `resumeRun(name, stepId)`, `cancelRun(name)` (each `frappeCall` to `konsol.orchestrator.api.start_run/get_run/retry_step/resume_run/cancel_run`), plus `onRunStep(cb)` wrapping `frappe.realtime?.on("orchestrator_step", cb)` and returning a no-op unsubscribe (guards `typeof frappe`/optional chaining when realtime absent). Test `konsol/tests/test_exec_api.py` — first `test_exec_*.py`: static-asserts each api.js fn + backend method string + realtime topic; surface tests for `get_run` (callable/name/signature, frappe-free); frappe-guarded behavioural `get_run` test (skipped on host). 11 passed / 1 skipped.

- **E5 — Exec state machine** (commit `feat(konsol-exec): E5 exec state machine (konsol#60)`, `d9c2c33`). New `konsol-exec/src/machines/runExecMachine.js` — XState v5 `setup().createMachine()`, context `{run, error, pendingStepId}`. States: `idle --LAUNCH--> launching --(launchRun=startRun)--> refreshing`; `refreshing` invokes `fetchRun=getRun` then `assignRun` (= `normalizeRun(event.output)` from `../orchestrator/runModel`) and branches on the `runSettled` guard (`isTerminal(context.run?.status)` from `../orchestrator/status`) → `done` when terminal else `watching`; `watching`/`done` handle `REFRESH` + `RUN_STEP` (→ refreshing), `RETRY_STEP`/`RESUME_FROM` (`rememberStep` then invoke `retryStepActor`/`resumeRunActor` → refreshing), `CANCEL` (→ `cancelRunActor` → refreshing). All effects are `fromPromise` actors over the api client (`startRun`/`getRun`/`retryStep`/`resumeRun`/`cancelRun`); zero data-shaping in the machine. Exported from `machines/index.js`. Static-assert test `konsol/tests/test_exec_machine.py` (18 tests) — file/export surface, api + pure-core imports, xstate scaffolding, all 5 events, all 5 actor calls, terminal settle.

- **E6 — Launch panel** (commit `feat(konsol-exec): E6 launch panel (konsol#60)`, `3776f16`). New `konsol-exec/src/components/ExecuteLaunch.jsx` — a thin React form (`export function ExecuteLaunch({ send, accent, busy })`). Fields: **Fiscal Year** (`fiscal_year`), **Fiscal Period** (`fiscal_period`), **Scope** (`scope`), **Pipeline Definition** (`definition`) text inputs + **Full Refresh** (`full_refresh`) and **Skip Airbyte Sync** (`skip_sync`) checkboxes, all held in one `React.useState` form object via a shared `set(key)` change handler (reads `e.target.checked` for checkboxes). The **"Start Run"** button calls `buildRunArgs(form)` (from `../orchestrator/params`) → `{definition, params}` and dispatches `send({ type: "LAUNCH", definition, params })` — matching the E5 `runExecMachine` `LAUNCH` event shape. Zero data-shaping in the component; logic lives in `buildRunArgs`. Static-assert test `konsol/tests/test_exec_launch.py` (15 tests) — file/export surface, React + `buildRunArgs` import, every field label+name, checkbox inputs, Start Run control, `buildRunArgs(` call, `LAUNCH` dispatch, `send` prop.

- **E7 — Live step timeline** (commit `feat(konsol-exec): E7 live step timeline (konsol#60)`, `62a827d`). New `konsol-exec/src/components/RunTimeline.jsx` — `export function RunTimeline({ run, send, accent })`. Renders the normalized view-model from the machine context (defensively re-`normalizeRun`s a raw doc): a **run header** (run `name` + a `statusTone(run.status)` pill from `../orchestrator/status` + a run-level **Cancel** → `send({ type: "CANCEL" })`), a **progress bar** from `progressPct(steps)` (from `../orchestrator/runModel`), and **one card per step** (`step.id`, `step.type`, a `statusTone` pill, `startedAt`→`endedAt`, `rows`, `output`/`error`). Per-step **Retry** → `send({ type: "RETRY_STEP", stepId: step.id })` and **Resume** → `send({ type: "RESUME_FROM", stepId: step.id })`. A `React.useEffect` subscribes via `onRunStep` (from `../api`) so realtime `orchestrator_step` events `send({ type: "RUN_STEP" })` (returns the unsubscribe for cleanup). Zero data-shaping in the component. Static-assert test `konsol/tests/test_exec_timeline.py` (20 tests) — file/export surface, react + pure-core + api imports, run-name header, statusTone/progressPct usage, per-step field rendering, all three controls + `stepId` payload, realtime refresh.

Backend orchestrator + P1/P2/P3 already complete on this branch. The two exec-plane planning docs (`HANDOFF-exec.md`, `PRDS-exec.md`) originated on `main` (commit 3b19ad5); they were brought onto `feat/orchestrator-p1` alongside E1 so PR konsol#60 is self-contained.

**Harness note:** `cd konsol-exec && node --test src/orchestrator/*.test.mjs` runs the pure core suite (13 tests, no npm install needed). Python regress: `python3 -m pytest konsol/tests/test_exec_*.py konsol/tests/test_orchestrator_*.py -q` (now 318 passed / 6 skipped; `test_exec_timeline.py` exists as of E7). The Execute view now has all three pieces — `ExecuteLaunch` (form → `LAUNCH`) and `RunTimeline` (live run + per-step controls) both wired to `runExecMachine` (importable from `../machines` barrel), which drives `REFRESH`/`RUN_STEP`/`RETRY_STEP`/`RESUME_FROM`/`CANCEL`. What remains is wiring the two components into `App.jsx` as a real navigable subview (E8).

## Next
**E8 — Wire into the app as the "Execute" view.** Compose the now-built launch + timeline pieces into a real navigable SPA subview driven by `runExecMachine`. This is a **React/app wiring** PRD → static-assertion pytest (no node --test; JSX isn't host-runnable). Touch `konsol-exec/src/App.jsx`, the sub-nav (`DomainSubNav` — likely in `App.jsx` or `components/`), and `konsol-exec/src/constants.js`:

1. Register an **`execute`** subview alongside the existing `setup`/`monitor`/`history` subviews (see how `konsolAppMachine` + `App.jsx` switch subviews and how `DomainSubNav` lists tabs; add the tab label/key in `constants.js` if that's where subview keys live).
2. In `App.jsx`, when the `execute` subview is active, instantiate `runExecMachine` (use the app's XState pattern — `useMachine` from `@xstate/react`, mirroring how other machines are wired), and render **`<ExecuteLaunch send={send} accent={…} busy={…} />`** + **`<RunTimeline run={state.context.run} send={send} accent={…} />`** together (launch on top, timeline below). Keep `App.jsx` thin — no data-shaping; pass machine `send`/`state.context.run` straight through.

Test: write/extend `konsol/tests/test_exec_app.py` — static-assert that `App.jsx` imports `ExecuteLaunch` + `RunTimeline` (+ `runExecMachine` from `../machines`), renders both components, passes `send` and `run`/`context.run`, and that an `execute` subview/tab is registered (string `"execute"` in `App.jsx`/`constants.js`/sub-nav). RED first (`python3 -m pytest konsol/tests/test_exec_app.py -q`), implement, GREEN. Regress BOTH suites (`cd konsol-exec && node --test src/orchestrator/*.test.mjs` AND `python3 -m pytest konsol/tests/test_exec_*.py konsol/tests/test_orchestrator_*.py -q`), commit `feat(konsol-exec): E8 execute view wiring (konsol#60)`, tick E8 in `PRDS-exec.md`, and repoint this Next section at **E9 — Delete the Desk page** (`konsol/pipeline/page/konsol_exec/` + `konsol/tests/test_orchestrator_spa_js.py`; assert the dir/files are gone and nothing references `pages/konsol_exec` or `frappe.pages["konsol-exec"]`).
