# Orchestrator build — durable handoff (read me first)

You are a fresh agent continuing the **konsol-exec orchestrator** build. This file is the single source of truth for context — you start with no memory of prior work. Read this, then `PRDS.md` (the backlog), then do **exactly one PRD** and update this file for the next agent.

## Where things are
- **Working dir:** `/home/pd/open_epm/docker/frappe/konsol`
- **Branch:** `feat/orchestrator-p1` (never commit to `main`)
- **Core package:** `konsol/orchestrator/` — pure-python, **no top-level `frappe` import** (must import & test on host without a bench)
- **Tests:** `konsol/tests/test_orchestrator_*.py`
- **Backlog + progress:** `konsol/orchestrator/PRDS.md` (checkboxes)
- **Design:** `open_epm/docs/developer-guide/design/konsol-exec-orchestrator.md`
- **Epic:** konsol#56 (P1 #57, P2 #58, P3 #59)

## The loop protocol (do ONE PRD, strict TDD)
1. Open `PRDS.md`, find the **first unchecked** `- [ ] **PRD-N`.
2. **Red:** write/extend `konsol/tests/test_orchestrator_<area>.py` with failing tests for that PRD. Run `python3 -m pytest konsol/tests/test_orchestrator_<area>.py -q` and confirm it fails.
3. **Green:** implement the code (under `konsol/orchestrator/`, or doctype JSON for doctype PRDs). Iterate until that file passes.
4. **Regress:** run the whole suite `python3 -m pytest konsol/tests/test_orchestrator_*.py -q` — all must pass.
5. **Commit** (format below).
6. Tick the PRD `- [x]` in `PRDS.md` and update the **Current state** + **Next** sections of this file. Commit that too (amend onto the same commit is fine).
7. Stop. Do not start the next PRD.

## Conventions
- **Commit message:**
  ```
  feat(orchestrator): PRD-N <short title> (konsol#57)

  <2-4 line what/why>. <count> TDD tests (<total> total).

  Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
  ```
- Pure-python core only in `dag.py`/`plan.py`/`state.py`/`executor.py`/`handlers.py`. If a PRD genuinely needs frappe (e.g. PRD-9 binding, PRD-10 API runtime), put frappe imports **inside functions**, and guard frappe-dependent tests with `frappe = pytest.importorskip("frappe")` so the host suite stays green.
- Doctype JSON lives at `konsol/<module>/doctype/<name>/<name>.json`. Tests for doctype PRDs just load the JSON and assert fields exist (no frappe needed). `pipeline_step` is under `konsol/pipeline/doctype/pipeline_step/`, `pipeline_run` under `konsol/pipeline/doctype/pipeline_run/`.
- Bash safety: no `$()` inside other commands' args; redirect to temp files; the `python3 -m pytest ...` form is fine.
- Keep changes scoped to the orchestrator. Do NOT touch `open_epm/dbt_project`, other repos, or unrelated konsol code.

## Current state (update me each PRD)
**Done:** PRD-1 … PRD-17. **P1 (#57) COMPLETE; P2 (#58) COMPLETE; P3 (#59) IN PROGRESS (2/4).** Suite: **217 tests passing** (+4 frappe-guarded skips on host). Next agent starts at **PRD-18** (third P3 item, #59).

**PRD-17 (Resource resolver — Airbyte/dbt/ClickHouse):** new **pure** module `konsol/orchestrator/resources.py` (no top-level frappe; degrade-to-no-op `whitelist` like `api.py`). Public API:
- `@dataclass Resource(kind, name, config)`.
- `REQUIRED_KEYS = {airbyte: (workspace_id, connection_id, destination_id), dbt: (project_dir,), clickhouse: (host, db)}`.
- `resolve(kind, params, settings) -> Resource` — for each required key picks `params[key]` first else `settings[key]` (via `_pick`; empty string counts as absent → falls through), so **params override settings** for the same key. Unknown kind → `ValueError` naming the kind + valid kinds; any missing required key(s) → `ValueError("{kind} resource missing required config: {keys}")`. Tolerates `params=None`/`settings=None`. `name` = `params['name']` | `settings['name']` | `kind`.
- Frappe-bound (function-local `import frappe`): `_epm_settings()` reads the **EPM Settings** single into the resolver's flat key shape (airbyte_workspace_id/airbyte_connection_id/airbyte_destination_id → workspace/connection/destination_id; dbt_project_path → project_dir; clickhouse_host → host; airbyte_clickhouse_database → db). `airbyte_resource()/dbt_resource()/clickhouse_resource(params=None)` = `resolve(kind, params, _epm_settings())`. `@whitelist() list_resources(params=None)` returns one `{kind, name, config, error}` per kind (resolve failures reported as `error`, not raised) for the SPA.
- Test: `konsol/tests/test_orchestrator_resources.py` (14 host tests + 1 guarded skip) — no top-level frappe import (source scan); `Resource` fields; `resolve` for all 3 kinds with complete settings; missing-key `ValueError` naming kind+key (airbyte destination_id, dbt project_dir, clickhouse db); unknown-kind error; precedence (params override + params fill missing setting); empty-string treated as missing; None params/settings; getters + `list_resources` callable with `__name__` preserved; frappe getter smoke guarded with `importorskip`. **No bench test yet** — a container smoke should confirm `list_resources()` reads EPM Settings and reports each kind's resolved config or a missing-config error. **Did NOT touch the `Connector` doctype** (PRD says build on it, don't duplicate; per-connector airbyte ids live on `Connector.airbyte_connection_id`/`airbyte_source_id` — a future enhancement could let `resolve`/getters merge a named Connector's ids into `settings`, but the canonical workspace/destination defaults already come off EPM Settings).

**PRD-16 (Per-step metrics + lineage):** new **pure** module `konsol/orchestrator/lineage.py` (no top-level frappe). Public API:
- `STEP_OUTPUTS: dict[step_id -> list[table]]` — static map mirroring `plan.DEFAULT_DEFINITION`: `extract→["epm_raw"]`, `seed→["epm_seeds"]`, `silver→["silver_*"]`, `gold→["gold_*"]`, `assertions→["close_assertions"]`, `signoff→[]`.
- `lineage_for(plan) -> list[(upstream, downstream)]` — builds `Dag(plan)`, emits dependency edges first (`dep -> step.id`, declaration order then depends_on order) then step→table edges (`step.id -> table` from `STEP_OUTPUTS`). For `DEFAULT_DEFINITION` that's the 5 dep edges + 5 step→table edges (signoff has none).
- `summarize(snapshot, metrics=None) -> {total_rows, duration_s, per_step, status}` — consumes `snapshot={step_id: status}` + `metrics={step_id: {rows, duration_s}}` (the numbers PRD-9 `FrappeSink` records on child rows). Sums rows + duration_s, builds one `{step_id, status, rows, duration_s}` per step in snapshot order (missing metrics → 0), and rolls status up via private `_rollup_status`: Failed if any Failed → Cancelled if any Cancelled → Running if any Running → Success if all in {Success,Skipped} → else Pending (empty snapshot → Pending). Decoupled from executor internals (just dicts); tolerant of `None` metrics.
- Test: `konsol/tests/test_orchestrator_lineage.py` (15 host tests) — no top-level frappe import (source scan); STEP_OUTPUTS for silver/gold/assertions/signoff; `lineage_for` 5 dep edges + step→table edges + signoff has no table edge; `summarize` totals (sum rows/duration), per-step shaping (order matches DEFAULT_DEFINITION, exact dict for one step), missing-metrics→0, rollup success/failed/running/skipped-as-success, empty-run shaping (Pending, []), None metrics. No frappe needed; no new fixture/hooks (pure module). **No API piece** (PRD said optional guarded getter is fine; not added — keep it pure for now).

**PRD-15 (Resume-from-step + retry API):** new **pure** module `konsol/orchestrator/resume.py` (no top-level frappe). Public API:
- `plan_resume(steps, statuses, from_step) -> {step_id: status}` — validates `from_step` exists in `steps` and the run is **settled** (via the shared `_prepare` helper: builds `Dag(steps)`, membership-checks the target, builds `RunState(dag, statuses)`, and requires `state.is_done()` i.e. nothing Running and nothing still runnable), then calls `state.resume_from(from_step)` and returns `state.snapshot()`. Inputs are not mutated (RunState copies statuses).
- `plan_retry(steps, statuses, step) -> {step_id: status}` — identical shape/validation via `state.retry(step)`.
- Both raise `ValueError` on an unknown step (`"unknown step: ..."`) or an unsettled run (`"run is not settled ..."`). A fresh all-Pending run is *unsettled* (root is runnable) → cannot resume/retry, by design.
- **`api.py` tightened (frappe-bound):** `retry_step(run_name, step_id)` / `resume_run(run_name, step_id)` now (1) rebuild `(steps, statuses)` from the run's persisted PRD-6 child rows via new helper `_steps_and_statuses(run_doc)` (parses each row's `step_id`/`step_type`/`depends_on` JSON array + `status`), (2) call `resume.plan_retry` / `resume.plan_resume` — which *validate settled + reset the subtree*, (3) persist the reset snapshot onto the child rows via `_persist_statuses(run_doc, snapshot)`, (4) set run `status="Queued"`, save+commit, then enqueue `run.run_pipeline(... retry_step=/resume_from=)` (PRD-9 seam, unchanged). **Wrapper param names kept as `step_id`** (not `from_step`) to preserve the existing PRD-10 `test_orchestrator_api.py` signature tests; the PRD's `from_step` is the pure-core arg name only.
- Test: `konsol/tests/test_orchestrator_resume.py` (12 host tests + 1 guarded skip) — no top-level frappe import (source scan); `plan_resume` resets target+descendants & preserves upstream, `runnable()` of the new snapshot == `[target]`, resume-from-root resets all; `plan_retry` re-arms a Failed step's subtree (settled run: gold=Failed, downstream Pending) & doesn't mutate input; `ValueError` on unknown step / unsettled run (a step Running) / fresh all-Pending run; api wrappers exist + signatures guarded with `importorskip("frappe")`. **No bench test yet** — a container smoke should confirm `retry_step`/`resume_run` on a real settled Pipeline Run reset the right child rows and the re-enqueued `run_pipeline` only re-executes the subtree.

**PRD-14 (Scheduling):** new **pure** module `konsol/orchestrator/cron.py` (no top-level frappe). Public API:
- `is_due(expr, now, last_run=None) -> bool` — minimal **5-field** cron matcher (minute hour dom month dow). `_parse_field(token, low, high)` expands each field to a set, supporting `*`, `*/n`, `a-b`, `a,b,c`, and bare literals (raises `ValueError` on out-of-range / empty / bad step). Day-of-week normalises Python's `Monday==0` to cron's `Sunday==0` and accepts both `0` and `7` for Sunday. The `last_run` guard returns `False` if `last_run` falls in the **same calendar minute** as `now` (blocks double-fire). `is_due` raises `ValueError` when the expression isn't exactly 5 fields.
- `run_due_schedules()` — frappe-bound (function-local `import frappe`; also imports `konsol.orchestrator.api`): `frappe.utils.now_datetime()`, `frappe.get_all("Pipeline Schedule", filters={enabled:1}, fields=[name, pipeline_definition, cron, params, last_run])`, for each due schedule calls `api.start_run(definition, params)` and stamps `last_run` via `frappe.db.set_value(..., update_modified=False)`, then commits. Bad cron → `frappe.log_error` + skip.
- New doctype **`konsol/pipeline/doctype/pipeline_schedule/pipeline_schedule.json`** (`istable: 0`, module Pipeline, `autoname: "field:schedule_name"`, `track_changes`): fields `schedule_name` (Data, reqd, unique), `pipeline_definition` (Link → Pipeline Definition, reqd), `cron` (Data, reqd), `params` (Code/JSON), `enabled` (Check, default 1), a `schedule_state_section` Section Break, then `last_run` (Datetime, read_only) + `next_run` (Datetime, read_only). Permissions System Manager + EPM Admin (rwcd). Has `__init__.py` + trivial `PipelineSchedule(Document)` controller. **Not registered as a fixture** (operator-authored, not seeded).
- **hooks.py** `scheduler_events.cron` gains `"* * * * *": ["konsol.orchestrator.cron.run_due_schedules"]`.
- Test: `konsol/tests/test_orchestrator_cron.py` (24 host tests + 1 guarded skip) — no top-level frappe import (source scan, indentation-aware); `is_due` truth table (`*/10` true at 0/10 false at 7; `*/30`; literals; specific hour/dom/month/dow incl. Sunday 0==7; list `0,15,30,45`; range `9-17`; combined `0,30 9-17 * * 1-5` Wed-vs-Sat); `last_run` same-minute block / prev-minute allow / None allows; wrong-field-count `ValueError`; doctype JSON field set + fieldtypes (`next_run` read_only); hooks.py references `run_due_schedules`; `run_due_schedules` callable guarded with `importorskip("frappe")`. **No bench test yet** — a container smoke should confirm an enabled Pipeline Schedule fires `start_run` once per due minute and `last_run` advances (no double-fire).

**PRD-13 (Definition→plan loader):** new pure module `konsol/orchestrator/definition.py` (no top-level frappe). Public API:
- `definition_to_steps(defn: dict) -> List[Step]` — iterates `defn["steps"]` (missing/None key → `[]`) preserving order; each row → `dag.Step(id=step_id, type=step_type, depends_on=..., params=...)`. `depends_on` via `_parse_depends_on` (None→`[]`, comma-split + strip + drop empties, list passes through — exactly the Group Close fixture format). `params` via `_parse_params` (None/""→`{}`, dict passthrough, JSON str decoded; malformed JSON or non-object JSON raises `ValueError` naming the step). `build_plan` already takes a step list, so it composes unchanged.
- `load_definition(name) -> List[Step]` — frappe-bound, **function-local** `import frappe`: `frappe.get_doc("Pipeline Definition", name).as_dict()` → `definition_to_steps`.
- Test: `konsol/tests/test_orchestrator_definition_loader.py` (14 host tests + 1 guarded skip) — module imports without frappe + no top-level frappe import (source scan); `definition_to_steps` on the Group Close fixture record yields the right ids/types/deps/params; comma-split+strip; JSON-str / dict / empty params; bad-json `ValueError`; missing `steps` key → `[]`; round-trips through `Dag(...).toposort()` (correct order) and `build_plan(...)` (matches `plan.DEFAULT_DEFINITION`, incl. `skip_sync` rewire); `load_definition` callable guarded with `importorskip("frappe")`. No new fixture/hooks needed (reuses PRD-12's `pipeline_definition.json`). **No bench test yet** — a container smoke should confirm `load_definition("Group Close")` returns the 6 steps after the PRD-12 fixture imports.

**PRD-12 (Pipeline Definition + Step Definition doctypes + Group Close fixture):** retires the path toward the hardcoded `plan.DEFAULT_DEFINITION` by letting users author DAGs as doctypes. New doctypes under `konsol/pipeline/doctype/`:
- **`pipeline_definition/pipeline_definition.json`** (`istable: 0`, module Pipeline, `autoname: "field:definition_name"`, `track_changes`): fields `definition_name` (Data, `reqd`, `unique`), `title` (Data), `enabled` (Check, default `"1"`), `description` (Small Text), `default_params` (Code/JSON — default run params: fiscal_year/fiscal_period/scope/full_refresh/skip_sync), a `steps_section` Section Break, then `steps` (Table → **Step Definition**). Permissions: System Manager + EPM Admin (rwcd).
- **`step_definition/step_definition.json`** (`istable: 1`, `editable_grid`, module Pipeline): fields `step_id` (Data, `reqd`, in_list_view), `step_type` (Select, `reqd`; options are the **9** built-ins `airbyte_sync/dbt_seed/dbt_run/dbt_build/dbt_test/close_assertions/signoff/cube_refresh/sql` — a superset of `handlers.BUILTIN_TYPES` (7); the extra `cube_refresh`/`sql` are forward-looking, no handlers yet), `depends_on` (Small Text — **comma-separated** step_ids, the format PRD-13's `definition_to_steps` will split), `params` (Code/JSON).
- Each doctype dir has the usual `__init__.py` + a trivial `Document` controller (`PipelineDefinition` / `StepDefinition`).
- **Fixture `konsol/fixtures/pipeline_definition.json`** seeds the canonical **Group Close** definition mirroring `plan.DEFAULT_DEFINITION`: steps extract(airbyte_sync)→seed(dbt_seed)→silver(dbt_run)→gold(dbt_run)→assertions(close_assertions)→signoff(signoff), each child row carrying `parentfield/parenttype` + comma-separated `depends_on`. `default_params: "{}"`. Registered in `hooks.py` `fixtures` list as `"Pipeline Definition"` (appended after "Gold Model"; the child table Step Definition rides along inside it — not its own fixture entry).
- Test: `konsol/tests/test_orchestrator_definition_doctypes.py` (19 tests) — pure host JSON-load, no frappe. Asserts both doctypes' field sets + fieldtypes; `definition_name` unique; `enabled` default 1; `steps` Table→Step Definition; `step_id` reqd; `step_type` Select options ⊇ `handlers.BUILTIN_TYPES` and include cube_refresh/sql; and the Group Close fixture decodes to step ids/types/dependency-edges that mirror `plan.DEFAULT_DEFINITION` + is registered in hooks.py. **No bench/migrate smoke test yet** — next agent or P2 wrap-up should `bench migrate` in a container to confirm the doctypes install and the fixture imports (every record has `name` per the fixtures convention).

**PRD-11 (konsol-exec SPA param form + timeline):** new Frappe **Desk Page** at `konsol/pipeline/page/konsol_exec/` (module **Pipeline**, route `/app/konsol-exec`) — the front-end over the PRD-10 whitelisted API. Files: `__init__.py`, `konsol_exec.json` (Page doctype def, `standard: "Yes"`, roles EPM Admin + System Manager), `konsol_exec.js` (vanilla Frappe `frappe.pages["konsol-exec"].on_page_load` → a `KonsolExecConsole` class). The page is **purely host-testable JS-presence** — no React, no build step, hand-authored so the strings the test asserts are stable.
- **Launch form** (`frappe.ui.FieldGroup`): `fiscal_year`/`fiscal_period` (Int), `scope` (Data, dbt select), `definition` (Data, pipeline definition), `full_refresh`/`skip_sync` (Check). `collect_params()` omits empty fiscal/scope, coerces flags to 0/1, returns `{definition, params}` matching `api.start_run(definition, params)`.
- **Start:** `page.set_primary_action("Start Run")` → `frappe.call("konsol.orchestrator.api.start_run", {definition, params})` → on `r.message` (the run name) calls `load_run(name)`.
- **Timeline:** `load_run` → `frappe.db.get_doc("Pipeline Run", run_name)` → `render_run` → `render_timeline(doc.steps)` → `render_step` per child row, surfacing PRD-6 fields `step_id`, `step_type`, `status` (as a coloured `indicator-pill`), `started_at`/`ended_at`, `rows`, `output`, `error`.
- **Actions:** each step row gets Retry / Resume / Cancel buttons → `api.retry_step(run_name, step_id)` / `api.resume_run(run_name, step_id)` / `api.cancel_run(run_name)`, each reloading the run after. Cancel shows the honest "takes effect at the next step" alert (the P2 cooperative-cancel caveat from PRD-10).
- **Live updates:** `bind_realtime()` → `frappe.realtime.on("orchestrator_step", ...)` (the event `FrappeSink` emits) → reloads the current run so the timeline reflects the persisted child rows.
- **Status indicators:** `indicator_html(status)` maps Success→green, Failed→red, Running→blue, Queued/Cancelled→orange/red, Pending/Skipped→gray.
- Test: `konsol/tests/test_orchestrator_spa_js.py` (14 tests) — loads the page JS + JSON and asserts: package `__init__.py` + JS + Page-doctype JSON (module Pipeline, standard Yes) exist; the `on_page_load` hook; all six form fields; the four `konsol.orchestrator.api.*` call sites; the six timeline fields; a `timeline` render fn; the `frappe.realtime.on("orchestrator_step", …)` subscription; retry/resume/cancel buttons; Success/Failed/Running indicators. No frappe / no browser. **The page has no browser/bench test — it needs a container/Desk smoke test (next agent / P2): confirm the page registers, `start_run` enqueues, and the realtime timeline updates live.**
- **NOTE for next agent:** there is ALSO a separate pre-built React bundle at `konsol/public/konsol_exec/konsol_exec.js` (minified, ~120k tokens) served by `konsol/www/konsol-exec.py` + `www/konsol-exec.html` at the website route `/konsol-exec`. That is a *different, build-artifact* front-end (not hand-editable, no source in this repo) and was **left untouched**. PRD-11 deliberately added a separate hand-authored Desk Page at `/app/konsol-exec` so the TDD JS-presence test has a stable, editable target. If the project later wants the orchestrator UI inside the React SPA, that's a build-pipeline task outside this host-test loop.

**PRD-10 (Whitelisted API):** new `konsol/orchestrator/api.py` — the thin frappe layer the SPA (PRD-11) + external callers hit. Imports on host **without frappe** via a degrade-to-no-op `whitelist` decorator: `try: import frappe; whitelist = frappe.whitelist except Exception: whitelist = <no-op deco>`. In a bench the real `frappe.whitelist` registers the routes; on host the no-op keeps `fn.__name__` intact. Every other frappe call is **inside** the functions. Public API (all `@whitelist()`):

**PRD-10 (Whitelisted API):** new `konsol/orchestrator/api.py` — the thin frappe layer the SPA (PRD-11) + external callers hit. Imports on host **without frappe** via a degrade-to-no-op `whitelist` decorator: `try: import frappe; whitelist = frappe.whitelist except Exception: whitelist = <no-op deco>`. In a bench the real `frappe.whitelist` registers the routes; on host the no-op keeps `fn.__name__` intact. Every other frappe call is **inside** the functions. Public API (all `@whitelist()`):
- `start_run(definition=None, params=None) -> run_name` — `_coerce_params` (dict | JSON str via `frappe.parse_json` | None → `{}`), `frappe.get_doc({"doctype":"Pipeline Run", status:"Queued", triggered_by, started_at, pipeline_definition, + the PRD-7 fields fiscal_year/fiscal_period/scope/full_refresh/skip_sync})`, `insert(ignore_permissions=True)` + commit, then `frappe.enqueue("konsol.orchestrator.run.run_pipeline", queue="default", timeout=1800, run_name=...)`. Returns the name immediately (heavy work runs on the worker).
- `retry_step(run_name, step_id) -> run_name` — set run `status="Queued"`, save+commit, `enqueue(... run_name=, retry_step=step_id)`.
- `resume_run(run_name, step_id) -> run_name` — same shape but `enqueue(... resume_from=step_id)`.
- `cancel_run(run_name) -> run_name` — first cut: set run `status="Cancelled"` + save/commit. **Limitation (honest):** `Executor.cancel()`/`.cancelled` only stop a *live* in-process executor in the worker; this persisted status stops a not-yet-started run but does NOT cooperatively interrupt a run already mid-flight in a worker. Robust cross-worker cooperative cancel = follow-up (P2).
- **`run.run_pipeline` extended** to `run_pipeline(run_name, retry_step=None, resume_from=None)`: when either is given it rebuilds the state from persisted child rows via the new pure `run.state_from_rows(dag, rows)` and calls `state.retry(retry_step)` / `state.resume_from(resume_from)` before driving the executor. Fresh runs (no kwargs) behave exactly as before. The existing `FrappeSink` reuses the persisted rows by `step_id` (no dupes), so retried/resumed steps update in place.
- **New pure helper `run.state_from_rows(dag, rows) -> RunState`** — maps each persisted PRD-6 child row's `step_id`→`status` onto a freshly-planned `Dag` (rows may be frappe child docs **or** dicts via the existing `_row_get`; rows whose `step_id` isn't in the plan are ignored; missing steps stay `Pending`; tolerant of `rows=None`). This is the seam retry/resume rebuild on.
- Test: `konsol/tests/test_orchestrator_api.py` (13 tests) — `api` imports without frappe; all four entrypoints exist/callable + `__name__` preserved + signatures (`definition/params`, `run_name/step_id`, `run_name`); `run_pipeline` exposes `retry_step`/`resume_from` kwargs; `state_from_rows` mapping (statuses, unknown-step ignore, dict rows, None) + retry/resume on the rebuilt state. No frappe needed. **The frappe-bound `start_run`/`retry_step`/`resume_run`/`cancel_run` bodies + `run_pipeline` resume path have no host test — they need a container smoke test (next agent / PRD-11 wiring).**

**PRD-9 (Frappe executor binding):** new `konsol/orchestrator/run.py` ties the pure core to a **Pipeline Run** doc — still imports host-side without frappe (every frappe/subprocess side effect is *inside* a function). Public API:
- `params_from_doc(doc) -> dict` (pure, tolerant of a doc **or** a bare dict) — maps the PRD-7 fields to `build_plan` keys: `fiscal_year`/`fiscal_period` via `_int_or_none` (0/""/None → `None`, so `build_plan` omits the dbt var), `scope` (`""`→`None`), `full_refresh`/`skip_sync` → `bool`, `pipeline_definition` (`""`→`None`).
- `plan_run(params) -> (Dag, RunState)` (pure) — `build_plan(DEFAULT_DEFINITION, params)` → `Dag` → `RunState`.
- `FrappeSink(run_doc, child_field="steps", publish=None, now=None)` — the executor observer (`on_step_start`/`on_step_result`). Upserts a child row per step in the run's `steps` table by `step_id` (no dupes; caches rows in `self._rows`, falls back to scanning `run_doc.steps`, then `run_doc.append("steps", …)` or list-append if the doc has no `.append`). Writes PRD-6 fields: `step_id`, `step_type`, `status` (Running→Success/Failed), `started_at`/`ended_at` (via injectable `now`), `rows`, `output` (=`result.log`), `error`. If `publish` is given, emits `publish("orchestrator_step", {step_id, step_type, status})`. Tolerant row accessors `_row_get`/`_row_set` work on a frappe child doc (attrs) **or** a dict.
- `make_runner(run_doc, params) -> runner(argv) -> StepResult` — the runtime injected into the executor; dispatches on `argv[0]`: `"dbt"` → `_run_dbt` (subprocess in `frappe.conf.dbt_project_dir` or `<site>/../dbt_project`), `"airbyte_sync"` → `_run_airbyte_sync` (calls `konsol.services.airbyte_service.run_airbyte_sync` + writes `EPM Settings.last_sync_at`), `"close_assertions"`/`"signoff"` → frappe-side stubs. **All these `_run_*` helpers import frappe/subprocess inside the function** — verify their import paths against the real services when wiring the container smoke test (`run_airbyte_sync`/`run_close_assertions` names are best-effort and may need adjusting).
- `run_pipeline(run_name) -> RunState` — enqueue-able entrypoint: `frappe.get_doc("Pipeline Run", run_name)` → `params_from_doc` → `plan_run` → set status Running+save → `Executor(handlers, sink=FrappeSink(doc, publish=<publish_realtime>), runner=make_runner(doc,params)).run(state)` → set final status Success/Failed + save + commit.
- **Executor/StepContext change:** `StepContext(step, runner=None)` now carries `.runner`; `Executor(registry, sink=None, runner=None)` stores `runner` and passes it into each `StepContext` it builds. Backward compatible (defaults `None` → handlers stay in pure command-builder mode on host). This is the seam PRD-8 handlers use (`ctx.runner(argv) -> StepResult`).
- Test: `konsol/tests/test_orchestrator_run_binding.py` (15 tests) — module imports without frappe; `params_from_doc` mapping (incl. 0→None, dict input); `plan_run` default/skip_sync/full_refresh; `FrappeSink` row create/update/failure/no-prior-start/publish; executor runner injection; full integration (drive `Executor(handlers, sink, runner)` over `plan_run({skip_sync})`, assert all 5 child rows Success + dbt argv hit the runner). No frappe needed. **The frappe-bound `run_pipeline`/`make_runner` paths have no host test — they need a container smoke test (next agent or PRD-10).**

**PRD-8 (Real handlers / command building):** `konsol/orchestrator/handlers.py` now ships real handlers instead of no-op stubs, while staying pure-python (no top-level frappe). New public API:
- `build_dbt_command(verb, params) -> list[str]` — the **pure testable core**. Maps `["dbt", verb]` + `--select <select>` (skipped if falsy/empty) + `--full-refresh` (if `params["full_refresh"]`) + `--vars <json>` (`json.dumps(vars, sort_keys=True)` → deterministic). Mirrors the param keys `plan.build_plan` emits (`select`, `full_refresh`, `vars`).
- `DBT_VERB_BY_TYPE` = `{dbt_seed: seed, dbt_run: run, dbt_build: build, dbt_test: test}` (mirrors `plan.DBT_TYPES`).
- Handlers read ctx via tolerant accessors `_params(ctx)` / `_runner(ctx)` (`_ctx_get` works on a `StepContext` **or** a bare `dict`, so the existing `get(t)({})` host tests still pass). dbt handlers (`_make_dbt_handler`) build the argv; if `ctx` has **no `runner`** they return `StepResult(ok=True, log="<joined cmd>")` (planning / host-safe), otherwise they delegate to `runner(argv) -> StepResult`. `airbyte_sync` / `close_assertions` / `signoff` are the same shape (no runner → ok no-op; runner present → `runner([type]) -> StepResult`). **Real subprocess/Airbyte/frappe execution is injected as that `runner` by PRD-9** — `StepContext` does not set `.runner` yet, so on host everything is a pure command-builder.
- Test: `konsol/tests/test_orchestrator_dbt_cmd.py` (16 tests) — builder output for select/full_refresh/vars/combined/empty, verb map, all builtins resolve + are host-safe with `{}`, dbt handler writes cmd into `log`, and an injected `runner` receives the exact argv. No frappe needed.

**PRD-7 (Pipeline Run params):** `konsol/pipeline/doctype/pipeline_run/pipeline_run.json` now carries a new "Run Parameters" section (inserted right before the `extract_section` Section Break) with the run-level fields the orchestrator feeds into `build_plan`: `pipeline_definition` (Data, `in_standard_filter`), `fiscal_year` (Int, `in_standard_filter`), `fiscal_period` (Int), `scope` (Data), then a Column Break (`params_col_break`) and the two flags `full_refresh` (Check, default 0), `skip_sync` (Check, default 0). These mirror `plan.build_plan` param keys (`skip_sync`, `full_refresh`, `scope`→`select`, `fiscal_year`/`fiscal_period`→`vars`). `pipeline_run.json` has **no `field_order`** — plain `fields` list (like `pipeline_step.json`), so fields were inserted positionally; all pre-existing fields untouched. Test: `konsol/tests/test_orchestrator_pipelinerun_doctype.py` (10 tests) loads the JSON and asserts field presence + fieldtypes + labels; no frappe needed.

**PRD-6 (Run Step doctype fields):** `konsol/pipeline/doctype/pipeline_step/pipeline_step.json` now carries the orchestrator per-step persistence fields appended after `output`: `step_id` (Data), `step_type` (Data), `depends_on` (Small Text, JSON array of upstream ids), `params` (Code/options JSON), `retry_count` (Int, default 0), `started_at` (Datetime), `ended_at` (Datetime), `error` (Small Text). All `read_only: 1`. Pre-existing grid fields (`stage`, `step`, `status`, `rows`, `duration`, `output`) untouched. The doctype has **no `field_order`** — it's a plain `fields` list, so new fields were just appended. Test: `konsol/tests/test_orchestrator_runstep_doctype.py` (12 tests) loads the JSON with `json.load` and asserts field presence + fieldtypes; no frappe needed.

**Module map / public API already built:**
- `executor.py` — `StepContext(step)` (exposes `.step` + `.params`); `Executor(registry, sink=None)` drives a `RunState` to a settled state via `.run(state) -> state`. Loop: while not `is_done()` and not cancelled, take the first `state.runnable()`, mark `Running`, build `StepContext`, call `registry.get(step.type)(ctx)`, mark `Success`/`Failed` from `result.ok`, notify sink. Handler exceptions → `StepResult(ok=False, error=str(exc))` (no crash). `registry` = anything with `.get(type)` (the `handlers` module works directly). `sink` is optional/duck-typed: `on_step_start(step)`, `on_step_result(step, result)` (both checked via `hasattr`). `.cancel()` sets a flag (`.cancelled` property) that stops launching new steps. Failure leaves descendants blocked (state machine handles it). Pure-python; **real Frappe sink + enqueue binding is PRD-9.**
- `dag.py` — `Step(id, type, depends_on=[], params={})`; `Dag(steps)` with `.get(id)`, `.roots()`, `.dependents(id)`, `.descendants(id)`, `.toposort()`; raises `DagError` on cycle / unknown dep / duplicate id.
- `plan.py` — `DEFAULT_DEFINITION` (extract→seed→silver→gold→assertions→signoff); `build_plan(definition, params)` applies `skip_sync` (drop airbyte + rewire), `full_refresh` (on dbt_run/build), `scope`→`select` (transforms), `fiscal_year`/`fiscal_period`→`vars`. Non-mutating. Type sets: `DBT_TYPES`, `DBT_TRANSFORM_TYPES`, `DBT_INCREMENTAL_TYPES`, `VARS_TYPES`.
- `state.py` — `Status` (Pending/Running/Success/Failed/Skipped/Cancelled); `RunState(dag, statuses=None)` with `.status(id)`, `.mark(id, s)`, `.runnable()`, `.running()`, `.failed()`, `.has_failed()`, `.is_done()`, `.is_success()`, `.retry(id)`, `.resume_from(id)`, `.snapshot()`. Skipped deps satisfy dependents; failed deps block descendants.
- `handlers.py` — `StepResult(ok, rows=0, log="", error="")` dataclass; `Handler` runtime-checkable Protocol `__call__(ctx)->StepResult`; `register(type)` decorator (raises `ValueError` on dup) over `_REGISTRY`; `get(type)` (raises `KeyError` if missing); `registered_types()`; `BUILTIN_TYPES` tuple — all 7 built-in types registered as **real (PRD-8)** handlers. `build_dbt_command(verb, params)->argv` (pure) + `DBT_VERB_BY_TYPE`; handlers build their command and delegate execution to an optional `ctx.runner` (none on host → return the planned command, `ok=True`). Tolerant ctx accessors (`StepContext` or bare dict). Pure-python; **the real `runner` (subprocess/Airbyte/frappe write-back of `last_sync_at`) is injected in PRD-9.**

## Next
**PRD-18 — FX surfacing (konsolidat#91 B/C).** Third **P3** item — commit subject stays `(konsol#59)`. New **pure** module `konsol/orchestrator/fx.py` (no top-level frappe): `build_fx_query(filters) -> str` — build safe SQL over `epm_silver.silver_exchange_rates` (and the gold lookup), filterable by from/to currency, as-of date, rate type, and source; currency codes MUST be validated against `^[A-Z]{3}$` (injection-safe — reject anything else). `normalize_fx_rows(rows, cols) -> [{from, to, rate, as_of, type, source}]` — shape raw CH result rows (handles the empty case). Then a frappe/ClickHouse-guarded `@whitelist() get_fx_rates(**filters)` using the existing `from konsol.clickhouse import execute` pattern (function-local import; check how `api.py`/other modules call CH). Part C (a manual-entry Exchange Rate doctype unioned in as an 'manual' erp_source) is a sub-item — **this PRD lands only the read view + query builder**, not the doctype. **Test (host):** `build_fx_query` emits the expected SQL per filter combo (no filter, from only, from+to, as-of, type, source) and is injection-safe (a bad currency code like `'; DROP` raises `ValueError`); `normalize_fx_rows` shaping incl. the empty-rows case. Guard the `get_fx_rates` API test with `frappe = pytest.importorskip("frappe")`. Strict TDD: write/extend `konsol/tests/test_orchestrator_fx.py` RED first. NOTE: check the real `epm_silver.silver_exchange_rates` column names + the inverse-rate work from konsolidat #107 (Memory mentions `silver_exchange_rates` only had `USD→JPY`; #107 synthesises inverse pairs) so the query/normalizer column mapping matches reality.
